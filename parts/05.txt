.h});clampCardToGuide(c);cards.push(c);selectedIds=new Set([c.id]);primaryId=c.id;markDirty();render()}
  function openCardEditor(id){const c=cards.find(x=>x.id===id);if(!c)return;editingCardId=id;editTitle.value=c.title;editBody.value=c.body;editDialog.showModal();setTimeout(()=>editTitle.focus(),0)}
  function commitCardEdit(){const c=cards.find(x=>x.id===editingCardId);if(!c)return;const title=editTitle.value.trim()||'カード',body=editBody.value;if(title===c.title&&body===c.body)return;pushHistory();c.title=title.slice(0,200);c.body=body.slice(0,4000);selectedIds=new Set([c.id]);primaryId=c.id;markDirty();render();setStatus('カード編集・未保存')}
  function deleteSelected(){if(!selectedIds.size)return;const blocked=selectedCards().filter(c=>c.locked||!c.deletable);if(blocked.length){setStatus(`削除できないカードが ${blocked.length} 枚あります`);return}pushHistory();cards=cards.filter(c=>!selectedIds.has(c.id));selectedIds.clear();primaryId=null;markDirty();render()}
  function duplicateSelected(){const src=selectedCards();if(!src.length)return;pushHistory();const made=src.map((s,i)=>{const c=safeCard({...clone(s),id:uid(),x:s.x+28,y:s.y+28,title:s.title+' コピー',locked:false,required:false,deletable:true,hideable:true,movable:true,resizable:true},i);clampCardToGuide(c);return c});cards.push(...made);selectedIds=new Set(made.map(c=>c.id));primaryId=made[0].id;markDirty();render()}
  function toggleLock(){const sel=selectedCards();if(!sel.length)return;pushHistory();const unlock=sel.every(c=>c.locked);sel.forEach(c=>c.locked=!unlock);markDirty();render()}
  function hideSelected(){const sel=selectedCards();if(!sel.length)return;const blocked=sel.filter(c=>c.locked||!c.hideable);if(blocked.length){setStatus(`非表示にできないカードが ${blocked.length} 枚あります`);return}pushHistory();sel.forEach(c=>c.hidden=true);selectedIds.clear();primaryId=null;markDirty();render()}
  function showCard(id){const c=cards.find(x=>x.id===id);if(!c)return;pushHistory();c.hidden=false;selectedIds=new Set([id]);primaryId=id;markDirty();render()}

  function startDrag(e,c,{toggleOnClick=false}={}){if(e.button!==0||!selectedIds.has(c.id))return;e.stopPropagation();primaryId=c.id;const target=e.currentTarget,canMove=!c.locked&&c.movable,movable=canMove?selectedCards().filter(x=>!x.locked&&x.movable):[],sx=e.clientX,sy=e.clientY,origin=new Map(movable.map(x=>[x.id,{x:x.x,y:x.y,w:x.w,h:x.h}])),base=origin.get(c.id);let moved=false,historyPushed=false;target.setPointerCapture(e.pointerId);
    const move=ev=>{if(!canMove||!base)return;const rawDx=ev.clientX-sx,rawDy=ev.clientY-sy;if(!moved&&Math.hypot(rawDx,rawDy)<4)return;if(!moved)moved=true;if(!historyPushed){pushHistory();historyPushed=true}let dx=rawDx/view.zoom,dy=rawDy/view.zoom;if(snapEnabled){dx=snap(base.x+dx)-base.x;dy=snap(base.y+dy)-base.y}if(constrainToGuide){const b=currentBounds();const minDx=Math.max(...movable.map(x=>b.left-origin.get(x.id).x)),maxDx=Math.min(...movable.map(x=>b.right-(origin.get(x.id).x+origin.get(x.id).w))),minDy=Math.max(...movable.map(x=>b.top-origin.get(x.id).y)),maxDy=Math.min(...movable.map(x=>b.bottom-(origin.get(x.id).y+origin.get(x.id).h)));dx=Math.min(Math.max(dx,minDx),maxDx);dy=Math.min(Math.max(dy,minDy),maxDy)}else{const minX=Math.min(...movable.map(x=>origin.get(x.id).x)),minY=Math.min(...movable.map(x=>origin.get(x.id).y));dx=Math.max(dx,-minX);dy=Math.max(dy,-minY)}movable.forEach(x=>{const o=origin.get(x.id);x.x=o.x+dx;x.y=o.y+dy;const n=stage.querySelector(`[data-id="${x.id}"]`);if(n){n.style.left=x.x+'px';n.style.top=x.y+'px'}});markDirty()};
    const end=ev=>{target.releasePointerCapture?.(ev.pointerId);target.removeEventListener('pointermove',move);target.removeEventListener('pointerup',end);target.removeEventListener('pointercancel',end);if(ev.type==='pointerup'&&!moved&&toggleOnClick){selectedIds.delete(c.id);primaryId=selectedIds.size?[...selectedIds][0]:null;refreshSelection()}};target.addEventListener('pointermove',move);target.addEventListener('pointerup',end);target.addEventListener('pointercancel',end)}
  function startResize(e,c,dir='se'){if(e.button!==0||c.locked||!c.resizable)return;e.preventDefault();e.stopPropagation();primaryId=c.id;const target=e.currentTarget,sx=e.clientX,sy=e.clientY,ox=c.x,oy=c.y,ow=c.w,oh=c.h,minW=140,minH=90;let changed=false,historyPushed=false;target.setPointerCapture(e.pointerId);const move=ev=>{const dx=(ev.clientX-sx)/view.zoom,dy=(ev.clientY-sy)/view.zoom;let left=ox,top=oy,right=ox+ow,bottom=oy+oh;if(dir.includes('w'))left=ox+dx;if(dir.includes('e'))right=ox+ow+dx;if(dir.includes('n'))top=oy+dy;if(dir.includes('s'))bottom=oy+oh+dy;if(snapEnabled){if(dir.includes('w'))left=snap(left);if(dir.includes('e'))right=snap(right);if(dir.includes('n'))top=snap(top);if(dir.includes('s'))bottom=snap(bottom)}const b=constrainToGuide?currentBounds():{left:0,top:0,right:Infinity,bottom:Infinity};if(dir.includes('w'))left=Math.max(b.left,Math.min(right-minW,left));if(dir.includes('e'))right=Math.min(b.right,Math.max(left+minW,right));if(dir.includes('n'))top=Math.max(b.top,Math.min(bottom-minH,top));if(dir.includes('s'))bottom=Math.min(b.bottom,Math.max(top+minH,bottom));const x=left,y=top,w=right-left,h=bottom-top;if(x===c.x&&y===c.y&&w===c.w&&h===c.h)return;if(!historyPushed){pushHistory();historyPushed=true}changed=true;c.x=x;c.y=y;c.w=w;c.h=h;const el=stage.querySelector(`[data-id="${c.id}"]`);if(el){el.style.left=c.x+'px';el.style.top=c.y+'px';el.style.width=c.w+'px';el.style.height=c.h+'px'}markDirty()};const end=ev=>{target.releasePointerCapture?.(ev.pointerId);target.removeEventListener('pointermove',move);target.removeEventListener('pointerup',end);target.removeEventListener('pointercancel',end);if(changed)refreshSelection()};target.addEventListener('pointermove',move);target.addEventListener('pointerup',end);target.addEventListener('pointercancel',end)}
  function align(kind){const sel=selectedCards().filter(c=>!c.locked&&c.movable);if(sel.length<2)return;pushHistory();if(kind==='left'){const x=Math.min(...sel.map(c=>c.x));sel.forEach(c=>c.x=x)}else if(kind==='top'){const y=Math.min(...sel.map(c=>c.y));sel.forEach(c=>c.y=y)}else if(kind==='hspace'){const a=[...sel].sort((x,y)=>x.x-y.x);if(a.length>2){const left=a[0].x,right=a.at(-1).x+a.at(-1).w,total=a.reduce((s,c)=>s+c.w,0),gap=Math.max(0,(right-left-total)/(a.length-1));let pos=left;a.forEach(c=>{c.x=snap(pos);pos+=c.w+gap})}}else if(kind==='vspace'){const a=[...sel].sort((x,y)=>x.y-y.y);if(a.length>2){const top=a[0].y,bottom=a.at(-1).y+a.at(-1).h,total=a.reduce((s,c)=>s+c.h,0),gap=Math.max(0,(bottom-top-total)/(a.length-1));let pos=top;a.forEach(c=>{c.y=snap(pos);pos+=c.h+gap})}}sel.forEach(clampCardToGuide);markDirty();render()}

  function applyTemplate(id,{confirmReplace=true}={}){const t=templateCatalog()[id];