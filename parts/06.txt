if(!t)return;if(confirmReplace&&cards.length&&!confirm('現在のカードをテンプレート内容に置き換えます。よろしいですか？'))return;pushHistory();paperSize=PAPER_FORMATS[t.paperSize]?t.paperSize:'B5';constrainToGuide=!!t.constrainToGuide;activeTemplateId=id;cards=(t.cards||[]).map((spec,i)=>{const def=resolveComponent(spec.componentKey||'generic',spec.source||'generic');return safeCard({id:uid(),componentKey:spec.componentKey||'generic',source:spec.source||'generic',title:spec.title||def.title,body:spec.body||def.body,w:spec.w||def.w,h:spec.h||def.h,x:spec.x??PAPER_ORIGIN.x+40,y:spec.y??PAPER_ORIGIN.y+40,locked:!!spec.locked,hidden:false,required:!!spec.required,deletable:spec.deletable!==false,hideable:spec.hideable!==false,movable:spec.movable!==false,resizable:spec.resizable!==false},i)});cards.forEach(clampCardToGuide);selectedIds.clear();primaryId=null;renderPaperOptions();renderTemplates();markDirty();render();setStatus('テンプレート適用・未保存')}
  function saveCustomTemplate(){const name=prompt('テンプレート名を入力してください');if(!name?.trim())return;const id='t'+Date.now();customTemplates[id]={label:name.trim().slice(0,80),note:'保存した業務テンプレートです。',paperSize,constrainToGuide,cards:cards.map(c=>({...clone(c),id:undefined,hidden:false}))};localStorage.setItem(TEMPLATE_KEY,JSON.stringify(customTemplates));activeTemplateId='custom:'+id;renderTemplates();setStatus('テンプレート保存済み')}
  function deleteCustomTemplate(){if(!activeTemplateId.startsWith('custom:'))return;const id=activeTemplateId.slice(7),t=customTemplates[id];if(!t)return;if(!confirm(`「${t.label}」を削除しますか？`))return;delete customTemplates[id];localStorage.setItem(TEMPLATE_KEY,JSON.stringify(customTemplates));activeTemplateId='builtin:free';renderTemplates();setStatus('保存テンプレートを削除しました')}
  function loadCustomTemplates(){try{const raw=localStorage.getItem(TEMPLATE_KEY);if(raw){const parsed=JSON.parse(raw);if(parsed&&typeof parsed==='object'&&!Array.isArray(parsed))customTemplates=parsed}}catch{customTemplates={}}}

  function fitView(){const f=currentFormat(),z=Math.min(.9,(viewport.clientWidth-100)/f.w,(viewport.clientHeight-100)/f.h);view.zoom=Math.max(.25,z);view.panX=viewport.clientWidth/2-(PAPER_ORIGIN.x+f.w/2)*view.zoom;view.panY=viewport.clientHeight/2-(PAPER_ORIGIN.y+f.h/2)*view.zoom;applyView()}
  function loadTrialSample(){applyTemplate('builtin:a4Form',{confirmReplace:true});requestAnimationFrame(fitView)}
  function resetLayout(){if(cards.length&&!confirm('現在のカードをすべて消して、自由レイアウトに戻します。よろしいですか？'))return;pushHistory();cards=[];selectedIds.clear();primaryId=null;paperSize='B5';constrainToGuide=false;activeTemplateId='builtin:free';guideVisible=true;snapEnabled=true;renderPaperOptions();renderTemplates();markDirty();render();requestAnimationFrame(fitView);setStatus('初期化・未保存')}
  function setZoom(next,clientX=viewport.getBoundingClientRect().left+viewport.clientWidth/2,clientY=viewport.getBoundingClientRect().top+viewport.clientHeight/2){next=Math.max(.25,Math.min(2,next));const r=viewport.getBoundingClientRect(),px=clientX-r.left,py=clientY-r.top,wx=(px-view.panX)/view.zoom,wy=(py-view.panY)/view.zoom;view.panX=px-wx*next;view.panY=py-wy*next;view.zoom=next;applyView()}
  viewport.addEventListener('wheel',e=>{e.preventDefault();const delta=Math.max(-120,Math.min(120,e.deltaY));const factor=Math.exp(-delta*0.000605);setZoom(view.zoom*factor,e.clientX,e.clientY)},{passive:false});
  const gesturePointers=new Map();let gestureBase=null;const pointDistance=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y),pointCenter=(a,b)=>({x:(a.x+b.x)/2,y:(a.y+b.y)/2});
  function resetGestureBase(){const pts=[...gesturePointers.values()];if(pts.length===1){const p=pts[0];gestureBase={mode:'pan',x:p.x,y:p.y,panX:view.panX,panY:view.panY}}else if(pts.length>=2){const a=pts[0],b=pts[1],center=pointCenter(a,b),r=viewport.getBoundingClientRect(),px=center.x-r.left,py=center.y-r.top;gestureBase={mode:'pinch',distance:Math.max(1,pointDistance(a,b)),zoom:view.zoom,worldX:(px-view.panX)/view.zoom,worldY:(py-view.panY)/view.zoom}}else gestureBase=null}
  viewport.addEventListener('pointerdown',e=>{if(e.target!==viewport&&e.target!==stage)return;e.preventDefault();if(!gesturePointers.size){selectedIds.clear();primaryId=null;render()}gesturePointers.set(e.pointerId,{x:e.clientX,y:e.clientY});viewport.setPointerCapture(e.pointerId);viewport.classList.add('panning');resetGestureBase()});
  viewport.addEventListener('pointermove',e=>{if(!gesturePointers.has(e.pointerId))return;gesturePointers.set(e.pointerId,{x:e.clientX,y:e.clientY});const pts=[...gesturePointers.values()];if(pts.length>=2){if(!gestureBase||gestureBase.mode!=='pinch')resetGestureBase();const a=pts[0],b=pts[1],center=pointCenter(a,b),r=viewport.getBoundingClientRect(),px=center.x-r.left,py=center.y-r.top,next=Math.max(.25,Math.min(2,gestureBase.zoom*(pointDistance(a,b)/gestureBase.distance)));view.zoom=next;view.panX=px-gestureBase.worldX*next;view.panY=py-gestureBase.worldY*next;applyView()}else if(pts.length===1){if(!gestureBase||gestureBase.mode!=='pan')resetGestureBase();const p=pts[0];view.panX=gestureBase.panX+(p.x-gestureBase.x);view.panY=gestureBase.panY+(p.y-gestureBase.y);applyView()}});
  function endViewportGesture(e){if(!gesturePointers.has(e.pointerId))return;gesturePointers.delete(e.pointerId);viewport.releasePointerCapture?.(e.pointerId);if(gesturePointers.size)resetGestureBase();else{gestureBase=null;viewport.classList.remove('panning')}}viewport.addEventListener('pointerup',endViewportGesture);viewport.addEventListener('pointercancel',endViewportGesture);

  function undo(){if(!undoStack.length)return;redoStack.push(snapshot());restoreSnapshot(undoStack.pop());markDirty()}
  function redo(){if(!redoStack.length)return;undoStack.push(snapshot());restoreSnapshot(redoStack.pop());markDirty()}
  function layoutPayload(){return {app:'business-layout-designer',version:7,cards:clone(cards),settings:{guideVisible,paperSize,snapEnabled,activeExtension,activeTemplateId,constrainToGuide}}}
  function save(){clearTimeout(autosaveTimer);try{lastManualSavedAt=