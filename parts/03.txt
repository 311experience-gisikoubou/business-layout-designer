
      {componentKey:'content',x:1270,y:300,w:910,h:380},
      {componentKey:'table',x:820,y:710,w:1360,h:380}
    ]}
  };

  const $=id=>document.getElementById(id);
  const viewport=$('viewport'),stage=$('stage'),statusEl=$('status'),zoomLabel=$('zoomLabel'),emptyTip=$('emptyTip'),paperGuide=$('paperGuide'),paperSizeEl=$('paperSize'),selectionInfo=$('selectionInfo'),snapBtn=$('snapBtn'),lockBtn=$('lockBtn'),hideBtn=$('hideBtn'),genericPalette=$('genericPalette'),extensionPalette=$('extensionPalette'),extensionPackEl=$('extensionPack'),packNote=$('packNote'),hiddenList=$('hiddenList'),importFile=$('importFile'),templateSelect=$('templateSelect'),templateNote=$('templateNote'),constraintBtn=$('constraintBtn'),deleteTemplateBtn=$('deleteTemplateBtn'),editDialog=$('editDialog'),editForm=$('editForm'),editTitle=$('editTitle'),editBody=$('editBody'),selectionBar=$('selectionBar'),selectionCount=$('selectionCount'),editSelectedBtn=$('editSelectedBtn'),quickDuplicateBtn=$('quickDuplicateBtn'),quickDeleteBtn=$('quickDeleteBtn'),clearSelectionBtn=$('clearSelectionBtn'),referenceImageEl=$('referenceImage'),referenceImportBtn=$('referenceImportBtn'),referenceImageFile=$('referenceImageFile'),referenceToggleBtn=$('referenceToggleBtn'),referenceRemoveBtn=$('referenceRemoveBtn'),referenceOpacity=$('referenceOpacity'),referenceOpacityValue=$('referenceOpacityValue'),referenceInfo=$('referenceInfo');
  let assets={},selectedPart=null;
  let cards=[],selectedIds=new Set(),primaryId=null,editingCardId=null,view={zoom:1,panX:70,panY:60},guideVisible=true,paperSize='B5',snapEnabled=true,activeExtension='none',activeTemplateId='free',constrainToGuide=false,customTemplates={},undoStack=[],redoStack=[],dirty=false,autosaveTimer=null,lastManualSavedAt=0,lastDraftSavedAt=0;
  let referenceMeta={visible:true,opacity:1},referenceRecord=null,referenceObjectUrl=null;
  const clone=v=>JSON.parse(JSON.stringify(v));
  const snapshot=()=>({cards:clone(cards),paperSize,constrainToGuide,activeTemplateId});
  const selectedCards=()=>cards.filter(c=>selectedIds.has(c.id));
  const snap=v=>snapEnabled?Math.round(v/GRID)*GRID:v;
  const setStatus=t=>statusEl.textContent=t;
  const formatClock=ts=>ts?new Date(ts).toLocaleTimeString('ja-JP',{hour:'2-digit',minute:'2-digit'}):'';
  function writeDraft(){if(!dirty)return;try{lastDraftSavedAt=Date.now();localStorage.setItem(DRAFT_KEY,JSON.stringify({...layoutPayload(),savedAt:lastDraftSavedAt,kind:'draft'}));setStatus(`自動保存済み ${formatClock(lastDraftSavedAt)}`)}catch{setStatus('自動保存できません・JSONバックアップ推奨')}}
  function scheduleAutosave(){clearTimeout(autosaveTimer);autosaveTimer=setTimeout(writeDraft,700)}
  const markDirty=()=>{dirty=true;setStatus('変更あり・自動保存待ち');scheduleAutosave()};
  const uid=()=>crypto.randomUUID?.()||('c'+Date.now()+Math.random());
  const currentFormat=()=>PAPER_FORMATS[paperSize]||PAPER_FORMATS.B5;
  const currentBounds=()=>({left:PAPER_ORIGIN.x,top:PAPER_ORIGIN.y,right:PAPER_ORIGIN.x+currentFormat().w,bottom:PAPER_ORIGIN.y+currentFormat().h});
  const normalizeReferenceMeta=v=>({visible:v?.visible!==false,opacity:Math.max(.15,Math.min(1,Number(v?.opacity)||1))});
  function openReferenceDb(){return new Promise((resolve,reject)=>{if(!('indexedDB'in window)){reject(new Error('IndexedDB unavailable'));return}const req=indexedDB.open(REFERENCE_DB_NAME,1);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(REFERENCE_STORE))db.createObjectStore(REFERENCE_STORE)};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error||new Error('IndexedDB open failed'))})}
  async function putReferenceRecord(record){const db=await openReferenceDb();try{await new Promise((resolve,reject)=>{const tx=db.transaction(REFERENCE_STORE,'readwrite');tx.objectStore(REFERENCE_STORE).put(record,REFERENCE_KEY);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error||new Error('IndexedDB write failed'));tx.onabort=()=>reject(tx.error||new Error('IndexedDB write aborted'))})}finally{db.close()}}
  async function getReferenceRecord(){const db=await openReferenceDb();try{return await new Promise((resolve,reject)=>{const tx=db.transaction(REFERENCE_STORE,'readonly'),req=tx.objectStore(REFERENCE_STORE).get(REFERENCE_KEY);req.onsuccess=()=>resolve(req.result||null);req.onerror=()=>reject(req.error||new Error('IndexedDB read failed'))})}finally{db.close()}}
  async function deleteReferenceRecord(){const db=await openReferenceDb();try{await new Promise((resolve,reject)=>{const tx=db.transaction(REFERENCE_STORE,'readwrite');tx.objectStore(REFERENCE_STORE).delete(REFERENCE_KEY);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error||new Error('IndexedDB delete failed'));tx.onabort=()=>reject(tx.error||new Error('IndexedDB delete aborted'))})}finally{db.close()}}
  function readImageSize(url){return new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve({width:img.naturalWidth,height:img.naturalHeight});img.onerror=()=>reject(new Error('画像を読み込めません'));img.src=url})}
  function setReferenceObjectUrl(blob){if(referenceObjectUrl)URL.revokeObjectURL(referenceObjectUrl);referenceObjectUrl=blob?URL.createObjectURL(blob):null;if(referenceObjectUrl)referenceImageEl.src=referenceObjectUrl;else referenceImageEl.removeAttribute('src')}
  function syncReferenceControls(){const has=!!referenceRecord;referenceToggleBtn.disabled=!has;referenceRemoveBtn.disabled=!has;referenceOpacity.disabled=!has;referenceToggleBtn.textContent=referenceMeta.visible?'表示 ON':'表示 OFF';referenceOpacity.value=String(referenceMeta.opacity);referenceOpacityValue.textContent=Math.round(referenceMeta.opacity*100)+'%';referenceInfo.textContent=has?(referenceRecord.width+'×'+referenceRecord.height+'px・縦横比維持・端末内のみ'):'未読込'}
  function renderReferenceImage(){const has=!!referenceRecord&&!!referenceObjectUrl,f=currentFormat();referenceImageEl.hidden=!has||!referenceMeta.visible;referenceImageEl.style.left=PAPER_ORIGIN.x+'px';referenceImageEl.style.top=PAPER_ORIGIN.y+'px';referenceImageEl.style.width=f.w+'px';referenceImageEl.style.height=f.h+'px';referenceImageEl.style.opacity=String(referenceMeta.opacity);syncReferenceControls()}
  async function importReferenceImage(file){try{if(!file||!REFERENCE_TYPES.has(file.type))throw new Error('PNG / JPEG / WebP を選んでください');if(file.size>REFERENCE_MAX_BYTES)throw new Error('画像は25MB以下にしてください');const url=URL.createObjectURL(file);let size;try{size=await readImageSize(url)}finally{URL.revokeObjectURL(url)}referenceRecord={blob:file,width:size.width,height:size.height,updatedAt:Date.now()};referenceMeta={visible:true,opacity:1};setReferenceObjectUrl(file);let persisted=true;try{await putReferenceRecord(referenceRecord)}catch{persisted=false}markDirty();render();setStatus(persisted?'基準画像を読込・端末内に保存':'基準画像を読込・このセッションのみ')}catch(err){alert('基準画像を読み込めませんでした。\n'+(err.message||err))}finally{referenceImageFile.value=''}}
  async function loadReferenceImageFromStore(){try{const stored=await getReferenceRecord();if(stored?.blob&&Number(stored.width)>0&&Number(stored.height)>0){referenceRecord=stored;setReferenceObjectUrl(stored.blob)}}catch{}render()}
  async function removeReferenceImage(){referenceRecord=null;setReferenceObjectUrl(null);try{await deleteReferenceRecord()}catch{}markDirty();render();setStatus('基準画像を外しました・元画像は変更していません')}

  function escapeHtml(value){return String(value).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]))}
  function componentButton(key,def,source){const b=document.createElement('button');b.type='button';b.innerHTML=`＋ ${escapeHtml(def.title)}<small>${escapeHtml(def.body)}</small>`;b.addEventListener('click',()=>addCard(key,source));return b}
  function resolveComponent(key,source){if(source==='generic')return GENERIC_COMPONENTS[key]||GENERIC_COMPONENTS.generic;return EXTENSION_PACKS[source]?.components?.[key]||GENERIC_COMPONENTS.generic}
  function normalizeRules(c){return {required:!!c.required,deletable:c.deletable!==false,hideable:c.hideable!==false,movable:c.movable!==false,resizable:c.resizable!==false}}
  function safeCardBase(c,index=0){if(!c||typeof c!=='object')throw new Error(`カード${index+1}が不正です`);const num=(v,f)=>Number.isFinite(Number(v))?Number(v):f,rules=normalizeRules(c),s=c.referenceSlice&&typeof c.referenceSlice==='object'?c.referenceSlice:null,referenceSlice=s?{x:Math.max(0,num(s.x,0)),y:Math.max(0,num(s.y,0)),w:Math.max(1,num(s.w,1)),h:Math.max(1,num(s.h,1)),sourceWidth:Math.max(1,num(s.sourceWidth,1)),sourceHeight:Math.max(1,num(s.sourceHeight,1))}:null,referenceStack=Array.isArray(c.referenceStack)?c.referenceStack.slice(0,20).map(item=>({x:Math.max(0,num(item?.x,0)),y:Math.max(0,num(item?.y,0)),w:Math.max(1,num(item?.w,1)),h:Math.max(1,num(item?.h,1)),dx:Math.max(0,num(item?.dx,0)),dy:Math.max(0,num(item?.dy,0)),dw:Math.max(1,num(item?.dw,1)),dh:Math.max(1,num(item?.dh,1)),sourceWidth:Math.max(1,num(item?.sourceWidth,1)),sourceHeight:Math.max(1,num(item?.sourceHeight,1))})):[];return {id:typeof c.id==='string'&&c.id?c.id:uid(),componentKey:typeof c.componentKey==='string'?c.componentKey:'legacy',source:typeof c.source==='string'?c.source:'generic',title:String(c.title??'カード').slice(0,200),body:String(c.body??'').slice(0,4000),x:Math.max(0,num(c.x,0)),y:Math.max(0,num(c.y,0)),w:Math.max(140,num(c.w,320)),h:Math.max(90,num(c.h,180)),locked:!!c.locked,hidden:!!c.hidden,referenceSlice,referenceStack,displayVariant:typeof c.displayVariant==='string'?c.displayVariant:'',...rules}}

  /* ---- parts-json-v1: internal selectable parts, local image assets ---- */
  const PART_TYPES=new Set(['image','heading','text','input','button','checkbox']);
  const PART_LABELS={image:'画像',heading:'見出し',text:'本文',input:'入力欄',button:'ボタン',checkbox:'チェック項目'};
  const JUSTIFY={left:'flex-start',center:'center',right:'flex-end'};
  const PART_IMAGE_MAX_BYTES=8*1024*1024;
  const isRefCard=c=>!!c.referenceSlice||(Array.isArray(c.referenceStack)&&c.referenceStack.length>0)||c.displayVariant==='patientCandidatesVertical'||c.displayVariant==='workRowsDense';
  function partBase(type,x,y,w,h,extra){return {partId:uid(),partType:type,x,y,width:w,height:h,text:'',imageRef:null,fontSize:13,fontWeight:400,alignment:'left',checked:false,...extra}}
  function defaultParts(c){if(isRefCard(c))return [];const w=c.w,h=c.h,p=[partBase('heading',26,0,Math.max(20,w-26-96),42,{text:c.title,fontSize:16,fontWeight:800}),partBase('text',12,54,Math.max(20,w-24),Math.max(20,h-66),{text:c.body})];if(c.componentKey==='image')p.push(partBase('image',12,54,Math.max(40,w-24),Math.max(40,h-66)));else if(c.componentKey==='form')p.push(partBase('input',12,54,Math.max(40,w-24),36,{text:'入力欄'}),partBase('checkbox',12,102,Math.max(40,w-24),28,{text:'確認項目'}),partBase('button',12,142,120,36,{text:'送信',alignment:'center',fontWeight:700}));return p}
  function normalizeParts(list,c){if(isRefCard(c))return [];if(!Array.isArray(list))return defaultParts(c);const num=(v,f)=>Number.isFinite(Number(v))?Number(v):f,seen=new Set();const out=list.slice(0,100).filter(p=>p&&typeof p==='object'&&PART_TYPES.has(p.partType)).map(p=>{const id=typeof p.partId==='string'&&p.partId&&!seen.has(p.partId)?p.partId:uid();seen.add(id);const fw=p.fontWeight==='bold'?700:p.fontWeight==='normal'?400:num(p.fontWeight,p.partType==='heading'?800:400);return {partId:id,partType:p.partType,x:Math.max(0,num(p.x,0)),y:Math.max(0,num(p.y,0)),width:Math.max(10,num(p.width,100)),height:Math.max(10,num(p.height,30)),text:String(p.text??'').slice(0,4000),imageRef:typeof p.imageRef==='string'&&p.imageRef?p.imageRef:null,fontSize:Math.min(96,Math.max(8,num(p.fontSize,p.partType==='heading'?16:13))),fontWeight:Math.min(900,Math.max(100,fw)),alignment:['left','center','right'].includes(p.alignment)?p.alignment:'left',checked:!!p.checked}});const d=defaultParts(c);['text','heading'].forEach(t=>{if(!out.some(p=>p.partType===t))out.unshift(d.find(p=>p.partType===t))});return out}
  function syncBaseParts(c){if(!Array.isArray(c.parts))return;const h=c.parts.find(p=>p.partType==='heading'),t=c.parts.find(p=>p.partType==='text');if(h)h.text=c.title;if(t)t.text=c.body}
  function syncCardFromPart(c,p){if(p.partType==='heading')c.title=(String(p.text).trim()||'カード').slice(0,200);else if(p.partType==='text')c.body=String(p.text).slice(0,4000)}
  function safeCard(c,index=0){if(!c||typeof c!=='object')throw new Error(`カード${index+1}が不正です`);const src={...c};if(!(typeof src.id==='string'&&src.id)&&typeof src.cardId==='string')src.id=src.cardId;if(src.w==null&&src.width!=null)src.w=src.width;if(src.h==null&&src.height!=null)src.h=src.height;if(typeof src.componentKey!=='string'&&typeof src.cardType==='string')src.componentKey=src.cardType;if(src.title==null&&Array.isArray(src.parts)){const hp=src.parts.find(p=>p&&p.partType==='heading'&&typeof p.text==='string');if(hp)src.title=hp.text}const card=safeCardBase(src,index);card.zIndex=Number.isFinite(Number(src.zIndex))?Number(src.zIndex):index+1;card.parts=normalizeParts(src.parts,card);if(Array.isArray(src.parts)&&card.parts.length){const hp=card.parts.find(p=>p.partType==='heading'),tp=card.parts.find(p=>p.partType==='text');if(hp)syncCardFromPart(card,hp);if(tp)syncCardFromPart(card,tp)}else syncBaseParts(card);return card}
  function exportCard(c,i){syncBaseParts(c);return {...clone(c),cardId:c.id,cardType:c.componentKey,width:c.w,height:c.h,zIndex:i+1}}
  function usedAssets(){const out={};cards.forEach(c=>(c.parts||[]).forEach(p=>{if(p.imageRef&&assets[p.imageRef])out[p.imageRef]=assets[p.imageRef]}));return out}
  function loadAssets(raw){if(!raw||typeof raw!=='object')return;const list=Array.isArray(raw)?raw:Object.values(raw);list.forEach(a=>{if(a&&typeof a.assetId==='string'&&a.assetId&&typeof a.dataUrl==='string'&&/^data:image\/(png|jpeg|webp);base64,/.test(a.dataUrl))assets[a.assetId]={assetId:a.assetId,name:String(a.name||'').slice(0,200),mimeType:String(a.mimeType||''),dataUrl:a.dataUrl,width:Number(a.width)||0,height:Number(a.height)||0}})}
  const partBar=$('partBar'),partInfo=$('partInfo'),partImageBtn=$('partImageBtn'),partImageFile=$('partImageFile'),partTextBtn=$('partTextBtn'),partCheckBtn=$('partCheckBtn'),partSize=$('partSize'),partBoldBtn=$('partBoldBtn'),partAlign=$('partAlign'),partBackBtn=$('partBackBtn');
  function findSelectedPart(){if(!selectedPart)return null;const c=cards.find(x=>x.id===selectedPart.cardId),p=c?.parts?.find(x=>x.partId===selectedPart.partId);return c&&p?{c,p}:null}
  function selectPart(cardId,partId){selectedIds=new Set([cardId]);primaryId=cardId;selectedPart={cardId,partId};refreshSelection()}
  function backToCard(){selectedPart=null;refreshSelection()}
  function syncPartBar(){const f=findSelectedPart();partBar.classList.toggle('show',!!f);if(!f)return;const {c,p}=f,img=p.partType==='image';partInfo.textContent=PART_LABELS[p.partType]+(c.locked?'（ロック中）':'');partImageBtn.hidden=!img;partTextBtn.hidden=img;partCheckBtn.hidden=p.partType!=='checkbox';partSize.hidden=img;partBoldBtn.hidden=img;partAlign.hidden=img;partSize.value=String(p.fontSize);partBoldBtn.classList.toggle('active',p.fontWeight>=600);partAlign.value=p.alignment}
  function editPart(fn,msg){const f=findSelectedPart();if(!f)return;if(f.c.locked){setStatus('ロック中のカードは編集できません');return}pushHistory();fn(f.p,f.c);syncCardFromPart(f.c,f.p);syncBaseParts(f.c);markDirty();render();setStatus(msg||'部品を編集・未保存')}
  function editPartText(){const f=findSelectedPart();if(!f)return;const {c,p}=f;if(p.partType==='heading'||p.partType==='text'){openCardEditor(c.id);return}if(p.partType==='image')return;if(c.locked){setStatus('ロック中のカードは編集できません');return}const t=prompt('文字を入力してください',p.text);if(t===null||t===p.text)return;editPart(q=>{q.text=t.slice(0,200)})}
  function readAsDataUrl(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result));r.onerror=()=>reject(new Error('画像を読み込めません'));r.readAsDataURL(file)})}
  async function replacePartImage(file,cardId,partId){try{if(!file||!REFERENCE_TYPES.has(file.type))throw new Error('PNG / JPEG / WebP を選んでください');if(file.size>PART_IMAGE_MAX_BYTES)throw new Error('画像は8MB以下にしてください');const c=cards.find(x=>x.id===cardId),p=c?.parts?.find(x=>x.partId===partId);if(!c||!p||p.partType!=='image')throw new Error('画像部品が見つかりません');if(c.locked)throw new Error('ロック中のカードは編集できません');const dataUrl=await readAsDataUrl(file),size=await readImageSize(dataUrl),assetId='asset-'+uid();pushHistory();assets[assetId]={assetId,name:String(file.name||'').slice(0,200),mimeType:file.type,dataUrl,width:size.width,height:size.height};p.imageRef=assetId;markDirty();render();setStatus('画像を差し替え・この部品のみ・未保存')}catch(err){alert('画像を差し替えできませんでした。\n'+(err.message||err))}finally{partImageFile.value=''}}
  function startPartGesture(e,c,p,n,resize){if(c.locked)return;const sx=e.clientX,sy=e.clientY,ox=p.x,oy=p.y,ow=p.width,oh=p.height;let moved=false;const target=e.currentTarget;target.setPointerCapture?.(e.pointerId);const move=ev=>{if(!moved&&Math.hypot(ev.clientX-sx,ev.clientY-sy)<4)return;if(!moved){pushHistory();moved=true}const dx=(ev.clientX-sx)/view.zoom,dy=(ev.clientY-sy)/view.zoom;if(resize){p.width=Math.round(Math.min(Math.max(20,ow+dx),Math.max(20,c.w-p.x)));p.height=Math.round(Math.min(Math.max(16,oh+dy),Math.max(16,c.h-p.y)));n.style.width=p.width+'px';n.style.height=p.height+'px'}else{p.x=Math.round(Math.min(Math.max(0,ox+dx),Math.max(0,c.w-p.width)));p.y=Math.round(Math.min(Math.max(0,oy+dy),Math.max(0,c.h-p.height)));n.style.left=p.x+'px';n.style.top=p.y+'px'}markDirty()};const end=ev=>{target.releasePointerCapture?.(ev.pointerId);target.removeEventListener('pointermove',move);target.removeEventListener('pointerup',end);target.removeEventListener('pointercancel',end)};target.addEventListener('pointermove',move);target.addEventListener('pointerup',end);target.addEventListener('pointercancel',end)}
  function partsLayerNode(c){const layer=document.createElement('div');layer.className='card-parts';(c.parts||[]).forEach(p=>{const n=document.createElement('div');n.className='part part-free part-'+p.partType+(selectedPart&&selectedPart.cardId===c.id&&selectedPart.partId===p.partId?' part-selected':'');n.dataset.partId=p.partId;n.style.cssText=`left:${p.x}px;top:${p.y}px;width:${p.width}px;height:${p.height}px;font-size:${p.fontSize}px;font-weight:${p.fontWeight};text-align:${p.alignment};justify-content:${JUSTIFY[p.alignment]}`;
      if(p.partType==='image'){const a=assets[p.imageRef];if(a){const img=document.createElement('img');img.src=a.dataUrl;img.alt=a.name||'';img.draggable=false;n.appendChild(img)}else{const s=document.createElement('span');s.textContent='画像：ここへドロップ／ダブルクリックで選択';n.appendChild(s)}
        n.addEventListener('dragover',e=>{if(e.dataTransfer?.types?.includes('Files')){e.preventDefault();n.classList.add('drop-over')}});n.addEventListener('dragleave',()=>n.classList.remove('drop-over'));n.addEventListener('drop',e=>{e.preventDefault();e.stopPropagation();n.classList.remove('drop-over');const f=e.dataTransfer?.files?.[0];if(f){selectPart(c.id,p.partId);replacePartImage(f,c.id,p.partId)}})}
      else if(p.partType==='checkbox'){const b=document.createElement('span');b.className='box';b.textContent=p.checked?'✓':'';const l=document.createElement('span');l.textContent=p.text;n.append(b,l)}
      else{const s=document.createElement('span');s.textContent=p.text;n.appendChild(s)}
      n.addEventListener('pointerdown',e=>{if(e.button!==0)return;if(!selectedPart||selectedPart.cardId!==c.id)return;e.stopPropagation();const isResize=!!e.target.closest('.part-resize');if(!isResize||!selectedPart||selectedPart.partId!==p.partId)selectPart(c.id,p.partId);startPartGesture(e,c,p,n,isResize)});
      n.addEventListener('dblclick',e=>{e.stopPropagation();selectPart(c.id,p.partId);if(p.partType==='image'){if(!c.locked)partImageFile.click()}else editPartText()});
      if(!c.locked){const r=document.createElement('div');r.className='part-resize';n.appendChild(r)}
      layer.appendChild(n)});return layer}

  function renderPaperOptions(){paperSizeEl.replaceChildren(...Object.entries(PAPER_FORMATS).map(([k,v])=>{const o=document.createElement('option');o.value=k;o.textContent=v.label;return o}));paperSizeEl.value=paperSize}
  function renderPalettes(){genericPalette.replaceChildren(...Object.entries(GENERIC_COMPONENTS).map(([k,v])=>componentButton(k,v,'generic')));extensionPackEl.replaceChildren(...Object.entries(EXTENSION_PACKS).map(([k,v])=>{const o=document.createElement('option');o.value=k;o.textContent=v.label;return o}));extensionPackEl.value=activeExtension;const pack=EXTENSION_PACKS[activeExtension]||EXTENSION_PACKS.none;packNote.textContent=pack.note;extensionPalette.replaceChildren(...Object.entries(pack.components).map(([k,v])=>componentButton(k,v,activeExtension)));extensionPalette.style.display=Object.keys(pack.components).length?'grid':'none'}
  function templateCatalog(){const built=Object.fromEntries(Object.entries(BUILTIN_TEMPLATES).map(([k,v])=>['builtin:'+k,{...v,builtin:true}]));const custom=Object.fromEntries(Object.entries(customTemplates).map(([k,v])=>['custom:'+k,{...v,builtin:false}]));return {...built,...custom}}
  function renderTemplates(){const catalog=templateCatalog();templateSelect.replaceChildren(...Object.entries(catalog).map(([id,t])=>{const o=document.createElement('option');o.value=id;o.textContent=(t.builtin?'':'★ ')+t.label;return o}));if(!catalog[activeTemplateId])activeTemplateId='builtin:free';templateSelect.value=activeTemplateId;const t=catalog[activeTemplateId]||catalog['builtin:free'];templateNote.textContent=(t.note||'保存したテンプレートです。')+(constrainToGuide?' 現在：範囲制約ON。':'');deleteTemplateBtn.disabled=t.builtin;syncConstraintUi()}
  function syncConstraintUi(){paperGuide.classList.toggle('constrained',constrainToGuide);constraintBtn.classList.toggle('active',constrainToGuide);constraintBtn.textContent=constrainToGuide?'範囲制約 ON':'範囲制約 OFF'}
  function updatePaperGuide(){const f=currentFormat();paperGuide.style.left=PAPER_ORIGIN.x+'px';paperGuide.style.top=PAPER_ORIGIN.y+'px';paperGuide.style.width=f.w+'px';pape