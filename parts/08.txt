emplateSelect.onchange=()=>{activeTemplateId=templateSelect.value;renderTemplates()};$('applyTemplateBtn').onclick=()=>applyTemplate(templateSelect.value);$('saveTemplateBtn').onclick=saveCustomTemplate;deleteTemplateBtn.onclick=deleteCustomTemplate;constraintBtn.onclick=()=>{pushHistory();constrainToGuide=!constrainToGuide;if(constrainToGuide)cards.forEach(clampCardToGuide);activeTemplateId='builtin:free';markDirty();renderTemplates();render()};
  window.addEventListener('keydown',e=>{const tag=e.target?.tagName,editing=tag==='INPUT'||tag==='TEXTAREA'||tag==='SELECT'||e.target?.isContentEditable;if(editing)return;if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='s'){e.preventDefault();save()}else if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='a'){e.preventDefault();selectedIds=new Set(cards.filter(c=>!c.hidden).map(c=>c.id));primaryId=[...selectedIds][0]||null;render()}else if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='d'){e.preventDefault();duplicateSelected()}else if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'){e.preventDefault();e.shiftKey?redo():undo()}else if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='y'){e.preventDefault();redo()}else if(e.key==='Escape'){if(selectedPart)backToCard();else{selectedIds.clear();primaryId=null;refreshSelection()}}else if(e.key==='Delete'||e.key==='Backspace'){if(!selectedPart)deleteSelected()}});
  window.addEventListener('dragover',e=>{if(e.dataTransfer?.types?.includes('Files'))e.preventDefault()});window.addEventListener('drop',e=>{if(e.dataTransfer?.types?.includes('Files'))e.preventDefault()});
  partBackBtn.onclick=backToCard;partImageBtn.onclick=()=>partImageFile.click();partImageFile.onchange=()=>{const f=partImageFile.files?.[0],s=selectedPart;if(f&&s)replacePartImage(f,s.cardId,s.partId);else partImageFile.value=''};partTextBtn.onclick=editPartText;partCheckBtn.onclick=()=>editPart(p=>{p.checked=!p.checked});partBoldBtn.onclick=()=>editPart(p=>{p.fontWeight=p.fontWeight>=600?400:700});partAlign.onchange=()=>editPart(p=>{p.alignment=['left','center','right'].includes(partAlign.value)?partAlign.value:'left'});partSize.onchange=()=>{const v=Math.min(96,Math.max(8,Number(partSize.value)||13));editPart(p=>{p.fontSize=v})};
  window.addEventListener('beforeunload',()=>{if(dirty)writeDraft()});

  load();renderPaperOptions();renderPalettes();renderTemplates();updatePaperGuide();render();loadReferenceImageFromStore();requestAnimationFrame(fitView);
})();
</script>
</body>
</html>
