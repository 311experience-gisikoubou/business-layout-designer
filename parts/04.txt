rGuide.style.height=f.h+'px';paperGuide.dataset.label=`${f.label}${constrainToGuide?'・範囲制約':''}`;paperGuide.classList.toggle('hidden',!guideVisible);syncConstraintUi()}

  function pushHistory(){undoStack.push(snapshot());if(undoStack.length>60)undoStack.shift();redoStack=[];syncButtons()}
  function restoreSnapshot(s){cards=(s.cards||[]).map(safeCard);if(PAPER_FORMATS[s.paperSize])paperSize=s.paperSize;if(typeof s.constrainToGuide==='boolean')constrainToGuide=s.constrainToGuide;if(s.activeTemplateId)activeTemplateId=s.activeTemplateId;selectedIds.clear();primaryId=null;renderPaperOptions();updatePaperGuide();renderTemplates();render()}
  function syncButtons(){const n=selectedIds.size,sel=selectedCards();$('undoBtn').disabled=!undoStack.length;$('redoBtn').disabled=!redoStack.length;$('duplicateBtn').disabled=!n;$('deleteBtn').disabled=!n;hideBtn.disabled=!n;lockBtn.disabled=!n;lockBtn.textContent=sel.length&&sel.every(c=>c.locked)?'🔓 ロック解除':'🔒 ロック';document.querySelectorAll('[data-align]').forEach(b=>b.disabled=(b.dataset.align==='hspace'||b.dataset.align==='vspace')?n<3:n<2);selectionInfo.textContent=n?`${n} 枚選択中${sel.some(c=>c.locked)?'（ロック含む）':''}${sel.some(c=>c.required)?'（必須含む）':''}`:'選択なし';selectionBar.classList.toggle('show',n>0);selectionCount.textContent=`${n}枚選択中`;editSelectedBtn.disabled=n!==1;quickDuplicateBtn.disabled=!n;quickDeleteBtn.disabled=!n||sel.some(c=>c.locked||!c.deletable);snapBtn.classList.toggle('active',snapEnabled);snapBtn.textContent=snapEnabled?'# 吸着 ON':'# 吸着 OFF';if(selectedPart&&!(selectedIds.size===1&&selectedIds.has(selectedPart.cardId)&&findSelectedPart()))selectedPart=null;syncPartBar()}
  function applyView(){stage.style.transform=`translate(${view.panX}px,${view.panY}px) scale(${view.zoom})`;zoomLabel.textContent=`${Math.round(view.zoom*100)}%`}
  function renderHiddenList(){const hidden=cards.filter(c=>c.hidden);if(!hidden.length){hiddenList.textContent='なし';return}hiddenList.replaceChildren(...hidden.map(c=>{const row=document.createElement('div');row.style.cssText='display:flex;align-items:center;gap:6px;margin:6px 0';const name=document.createElement('span');name.textContent=c.title;name.style.cssText='flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap';const b=document.createElement('button');b.type='button';b.className='mini';b.textContent='↩';b.title='再表示';b.onclick=()=>showCard(c.id);row.append(name,b);return row}))}
  function render(){stage.querySelectorAll('.card').forEach(n=>n.remove());cards.filter(c=>!c.hidden).forEach(c=>stage.appendChild(cardNode(c)));emptyTip.style.display=cards.some(c=>!c.hidden)?'none':'block';stage.classList.toggle('grid-off',!snapEnabled);renderHiddenList();syncButtons();applyView();updatePaperGuide();renderReferenceImage()}
  function selectCard(id,additive=false){if(additive){selectedIds.has(id)?selectedIds.delete(id):selectedIds.add(id)}else selectedIds=new Set([id]);primaryId=selectedIds.has(id)?id:(selectedIds.size?[...selectedIds][0]:null)}
  function refreshSelection(){syncButtons();stage.querySelectorAll('.card').forEach(el=>{el.classList.toggle('selected',selectedIds.has(el.dataset.id));el.classList.toggle('part-mode',!!selectedPart&&selectedPart.cardId===el.dataset.id)});stage.querySelectorAll('[data-part-id]').forEach(n=>n.classList.toggle('part-selected',!!selectedPart&&n.dataset.partId===selectedPart.partId))}
  function patientListNode(){const panel=document.createElement('div');panel.className='patient-list';const head=document.createElement('div');head.className='patient-list-head';for(const [cls,txt] of [['patient-list-icon','♙'],['patient-list-title','同一患者候補'],['patient-list-sub','同姓同名・類似情報の候補を確認'],['patient-list-more','候補を見る ›']]){const s=document.createElement('span');s.className=cls;s.textContent=txt;head.appendChild(s)}const rows=document.createElement('div');rows.className='patient-list-rows';[['佐藤 一郎','さくら歯科クリニック','患者番号 10023'],['佐藤 一郎','中央歯科医院','選択'],['佐藤 一郎','みどり歯科','選択']].forEach(([name,clinic,action],i)=>{const row=document.createElement('div');row.className='patient-row';const icon=document.createElement('span');icon.className='patient-person';icon.textContent='♙';const main=document.createElement('span');main.className='patient-main';const n=document.createElement('span');n.className='patient-name';n.textContent=name;const cl=document.createElement('span');cl.className='patient-clinic';cl.textContent=clinic;main.append(n,cl);const tail=document.createElement('span');tail.className=i===0?'patient-meta':'patient-select';tail.textContent=action;row.append(icon,main,tail);rows.appendChild(row)});panel.append(head,rows);return panel}
  function workListNode(){const panel=document.createElement('div');panel.className='work-list';const head=document.createElement('div');head.className='work-head';const icon=document.createElement('span');icon.className='work-icon';icon.textContent='▣';const title=document.createElement('span');title.className='work-title';title.textContent='技工内容入力';const actions=document.createElement('div');actions.className='work-actions';['＋ 行追加','コピー','行削除','☆ よく使う内容から選択'].forEach(t=>{const b=document.createElement('span');b.className='work-action';b.textContent=t;actions.appendChild(b)});head.append(icon,title,actions);const table=document.createElement('div');table.className='work-table';const cols=['No.','部位','技工内容','区分','材料・規格','数','状態',''];const h=document.createElement('div');h.className='work-tr work-th';cols.forEach(t=>{const s=document.createElement('span');s.className='work-td';s.textContent=t;h.appendChild(s)});table.appendChild(h);const rows=[['1','16','フルジルコニアクラウン','クラウン','ジルコニア','1'],['2','12','硬質レジン前装冠','前歯','レジン前装','1'],['3','36','インレー','インレー','ハイブリッド','1'],['4','45','クラウン','クラウン','ジルコニア','1'],['5','46','クラウン','クラウン','ジルコニア','1'],['6','11','前装冠','前歯','レジン前装','1'],['7','24','インレー','インレー','ハイブリッド','1'],['8','25','クラウン','クラウン','ジルコニア','1']];rows.forEach(r=>{const row=document.createElement('div');row.className='work-tr';r.forEach(v=>{const s=document.createElement('span');s.className='work-td';s.textContent=v;row.appendChild(s)});const status=document.createElement('span');status.className='work-td work-status';status.textContent='入力中';const del=document.createElement('span');del.className='work-td';del.textContent='⌫';row.append(status,del);table.appendChild(row)});const memo=document.createElement('div');memo.className='work-memo';const ml=document.createElement('span');ml.textContent='技工内容メモ';const mb=document.createElement('span');mb.className='work-memo-box';mb.textContent='技工上の注意点、特記事項などを入力…';memo.append(ml,mb);const footer=document.createElement('div');footer.className='work-footer';const clear=document.createElement('span');clear.className='work-footer-left';clear.textContent='⌫ 入力内容をクリア';const fa=document.createElement('div');fa.className='work-footer-actions';[['下書き保存',''],['納品書を発行','issue']].forEach(([t,x])=>{const b=document.createElement('span');b.className='work-footer-btn '+x;b.textContent=t;fa.appendChild(b)});footer.append(clear,fa);panel.append(head,table,memo,footer);return panel}
  function cardNode(c){const isReferenceCard=!!c.referenceSlice||(Array.isArray(c.referenceStack)&&c.referenceStack.length>0)||c.displayVariant==='patientCandidatesVertical',el=document.createElement('section');el.className='card'+(selectedIds.has(c.id)?' selected':'')+(c.locked?' locked':'')+(c.required?' required':'')+(!c.resizable?' no-resize':'')+(isReferenceCard?' reference-slice-card':'');el.dataset.id=c.id;if(selectedPart&&selectedPart.cardId===c.id)el.classList.add('part-mode');el.style.cssText=`left:${c.x}px;top:${c.y}px;width:${c.w}px;height:${c.h}px`;
    const resizeHandles=['n','ne','e','se','s','sw','w','nw'].map(dir=>{const h=document.createElement('div');h.className='resize-handle';h.dataset.dir=dir;h.setAttribute('aria-hidden','true');h.addEventListener('pointerdown',e=>startResize(e,c,dir));return h});
    if(c.displayVariant==='patientCandidatesVertical'){el.classList.add('patient-list-card');const panel=patientListNode(),label=document.createElement('div');label.className='reference-slice-label';label.textContent=c.title;el.append(panel,label,...resizeHandles)}
    else if(c.displayVariant==='workRowsDense'){el.classList.add('work-list-card');const panel=workListNode(),label=document.createElement('div');label.className='reference-slice-label';label.textContent=c.title;el.append(panel,label,...resizeHandles)}
    else if(Array.isArray(c.referenceStack)&&c.referenceStack.length&&referenceObjectUrl){const visual=document.createElement('div');visual.className='reference-slice-visual';const stackW=Math.max(...c.referenceStack.map(s=>s.dx+s.dw)),stackH=Math.max(...c.referenceStack.map(s=>s.dy+s.dh)),fitX=c.w/stackW,fitY=c.h/stackH;c.referenceStack.forEach(s=>{const pw=s.dw*fitX,ph=s.dh*fitY,piece=document.createElement('div');piece.style.cssText=`position:absolute;left:${s.dx*fitX}px;top:${s.dy*fitY}px;width:${pw}px;height:${ph}px;overflow:hidden`;const img=document.createElement('img');img.className='reference-slice-img';img.src=referenceObjectUrl;img.alt='';const scale=Math.min(pw/s.w,ph/s.h),offsetX=(pw-s.w*scale)/2,offsetY=(ph-s.h*scale)/2;img.style.width=(s.sourceWidth*scale)+'px';img.style.height=(s.sourceHeight*scale)+'px';img.style.left=(offsetX-s.x*scale)+'px';img.style.top=(offsetY-s.y*scale)+'px';piece.appendChild(img);visual.appendChild(piece)});const label=document.createElement('div');label.className='reference-slice-label';label.textContent=c.title;el.append(visual,label,...resizeHandles)}
    else if(c.referenceSlice&&referenceObjectUrl){const s=c.referenceSlice,visual=document.createElement('div');visual.className='reference-slice-visual';const img=document.createElement('img');img.className='reference-slice-img';img.src=referenceObjectUrl;img.alt='';const scale=Math.min(c.w/s.w,c.h/s.h),offsetX=(c.w-s.w*scale)/2,offsetY=(c.h-s.h*scale)/2;img.style.width=(s.sourceWidth*scale)+'px';img.style.height=(s.sourceHeight*scale)+'px';img.style.left=(offsetX-s.x*scale)+'px';img.style.top=(offsetY-s.y*scale)+'px';visual.appendChild(img);const label=document.createElement('div');label.className='reference-slice-label';label.textContent=c.title;el.append(visual,label,...resizeHandles)}
    else{const header=document.createElement('div');header.className='card-header';header.innerHTML='<span class="card-dot"></span>';const title=document.createElement('span');title.className='card-title';title.textContent=c.title;const hasH=!!c.parts?.some(p=>p.partType==='heading'),hasT=!!c.parts?.some(p=>p.partType==='text');if(!hasH)header.appendChild(title);
      if(c.source&&c.source!=='generic'){const source=document.createElement('span');source.className='badge';source.textContent=EXTENSION_PACKS[c.source]?.label||'拡張';header.appendChild(source)}if(c.required){const b=document.createElement('span');b.className='badge required-badge';b.textContent='必須';header.appendChild(b)}if(c.locked){const b=document.createElement('span');b.className='badge';b.textContent='🔒';header.appendChild(b)}
      const actions=document.createElement('div');actions.className='mini-actions';const edit=document.createElement('button');edit.className='mini';edit.textContent='✎';edit.title='内容を編集';edit.onclick=e=>{e.stopPropagation();openCardEditor(c.id)};const dup=document.createElement('button');dup.className='mini';dup.textContent='⧉';dup.title='複製';dup.onclick=e=>{e.stopPropagation();selectedIds=new Set([c.id]);primaryId=c.id;duplicateSelected()};const del=document.createElement('button');del.className='mini';del.textContent='×';del.title=c.deletable?'削除':'削除禁止';del.disabled=!c.deletable;del.onclick=e=>{e.stopPropagation();selectedIds=new Set([c.id]);primaryId=c.id;deleteSelected()};actions.append(edit,dup,del);header.appendChild(actions);
      const body=document.createElement('div');body.className='card-body';body.textContent=c.body;title.ondblclick=e=>{e.stopPropagation();openCardEditor(c.id)};body.ondblclick=e=>{e.stopPropagation();openCardEditor(c.id)};el.append(header,...(hasT?[]:[body]),partsLayerNode(c),...resizeHandles)}
    el.addEventListener('pointerdown',e=>{if(e.target.closest('.mini,.resize-handle'))return;if(e.button!==0)return;const additive=e.shiftKey,baseEl=e.target.closest('[data-base-part],.part-free');let wasSelected=selectedIds.has(c.id);if(selectedPart&&selectedPart.cardId===c.id&&!additive){if(baseEl){e.stopPropagation();selectPart(c.id,baseEl.dataset.partId);return}selectedPart=null;wasSelected=false}if(additive){selectedPart=null;selectCard(c.id,true);refreshSelection();if(!selectedIds.has(c.id))return}else if(!wasSelected){selectedPart=null;selectCard(c.id,false);refreshSelection()}startDrag(e,c,{toggleOnClick:wasSelected&&!additive,basePartId:baseEl?.dataset.partId||null})});return el}

  function clampCardToGuide(c){if(!constrainToGuide)return;const b=currentBounds();c.w=Math.min(c.w,b.right-b.left);c.h=Math.min(c.h,b.bottom-b.top);c.x=Math.min(Math.max(c.x,b.left),b.right-c.w);c.y=Math.min(Math.max(c.y,b.top),b.bottom-c.h)}
  function addCard(key,source='generic'){pushHistory();const t=resolveComponent(key,source),offset=cards.length*24,c=safeCard({id:uid(),componentKey:key,source,title:t.title,body:t.cardBody??t.body,x:snap(PAPER_ORIGIN.x+40+offset),y:snap(PAPER_ORIGIN.y+40+offset),w:t.w,h:t