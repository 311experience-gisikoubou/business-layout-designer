Date.now();localStorage.setItem(STORAGE_KEY,JSON.stringify({...layoutPayload(),savedAt:lastManualSavedAt,kind:'manual'}));localStorage.removeItem(DRAFT_KEY);lastDraftSavedAt=0;dirty=false;setStatus(`保存済み ${formatClock(lastManualSavedAt)}`)}catch{dirty=true;setStatus('保存できません・JSONバックアップ推奨')}}
  function exportJson(){const blob=new Blob([JSON.stringify({...layoutPayload(),exportedAt:new Date().toISOString()},null,2)],{type:'application/json'}),a=document.createElement('a'),d=new Date(),stamp=`${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}${String(d.getDate()).padStart(2,'0')}`;a.href=URL.createObjectURL(blob);a.download=`business-layout-${stamp}.json`;document.body.appendChild(a);a.click();const href=a.href;a.remove();setTimeout(()=>URL.revokeObjectURL(href),0);setStatus('JSON出力済み')}
  function importPayload(data){if(!data||typeof data!=='object'||!Array.isArray(data.cards))throw new Error('cards配列がありません');if(data.cards.length>500)throw new Error('カード数が500件を超えています');pushHistory();cards=data.cards.map(safeCard);const settings=data.settings&&typeof data.settings==='object'?data.settings:data;if(typeof settings.guideVisible==='boolean')guideVisible=settings.guideVisible;if(PAPER_FORMATS[settings.paperSize])paperSize=settings.paperSize;if(typeof settings.snapEnabled==='boolean')snapEnabled=settings.snapEnabled;if(EXTENSION_PACKS[settings.activeExtension])activeExtension=settings.activeExtension;if(typeof settings.constrainToGuide==='boolean')constrainToGuide=settings.constrainToGuide;if(typeof settings.activeTemplateId==='string')activeTemplateId=settings.activeTemplateId;cards.forEach(clampCardToGuide);selectedIds.clear();primaryId=null;renderPaperOptions();renderPalettes();renderTemplates();markDirty();render();setStatus('JSON読込済み・未保存')}
  async function importJsonFile(file){try{const text=await file.text();importPayload(JSON.parse(text))}catch(err){alert(`JSONを読み込めませんでした。\n${err.message||err}`)}finally{importFile.value=''}}
  function applyPaperSize(next,{warn=true}={}){if(!PAPER_FORMATS[next]||next===paperSize)return;pushHistory();const old=paperSize;paperSize=next;if(constrainToGuide){const moved=cards.filter(c=>{const before={x:c.x,y:c.y,w:c.w,h:c.h};clampCardToGuide(c);return before.x!==c.x||before.y!==c.y||before.w!==c.w||before.h!==c.h}).length;if(warn&&moved)setStatus(`${moved} 枚を新しい範囲内へ調整しました`)}else if(warn&&old!==next){const b=currentBounds(),outside=cards.filter(c=>c.x<b.left||c.y<b.top||c.x+c.w>b.right||c.y+c.h>b.bottom);if(outside.length)setStatus(`ガイド外のカード ${outside.length} 枚`)}renderPaperOptions();markDirty();render()}
  function migrateCard(c){return safeCard({...c,componentKey:c.componentKey||'legacy',source:c.source||'generic'})}
  function applyStoredPayload(s){const settings=s.settings&&typeof s.settings==='object'?s.settings:s;if(Array.isArray(s.cards))cards=s.cards.map(migrateCard);if(typeof settings.guideVisible==='boolean')guideVisible=settings.guideVisible;if(PAPER_FORMATS[settings.paperSize])paperSize=settings.paperSize;if(typeof settings.snapEnabled==='boolean')snapEnabled=settings.snapEnabled;if(EXTENSION_PACKS[settings.activeExtension])activeExtension=settings.activeExtension;if(typeof settings.constrainToGuide==='boolean')constrainToGuide=settings.constrainToGuide;if(typeof settings.activeTemplateId==='string')activeTemplateId=settings.activeTemplateId}
  function load(){loadCustomTemplates();let manual=null,draft=null;try{const raw=localStorage.getItem(STORAGE_KEY);if(raw)manual=JSON.parse(raw)}catch{}try{const raw=localStorage.getItem(DRAFT_KEY);if(raw)draft=JSON.parse(raw)}catch{}lastManualSavedAt=Number(manual?.savedAt)||0;lastDraftSavedAt=Number(draft?.savedAt)||0;const useDraft=!!draft&&(!manual||lastDraftSavedAt>=lastManualSavedAt);const chosen=useDraft?draft:manual;if(chosen)applyStoredPayload(chosen);dirty=useDraft;setStatus(useDraft?`自動保存から復元 ${formatClock(lastDraftSavedAt)}`:(cards.length?`保存済み ${formatClock(lastManualSavedAt)}`:'未保存'))}
  function restoreManualSave(){let manual=null;try{const raw=localStorage.getItem(STORAGE_KEY);if(raw)manual=JSON.parse(raw)}catch{}if(!manual){setStatus('確定保存がありません');return}if(dirty&&!confirm('現在の変更を破棄して、最後に「保存」した状態へ戻します。よろしいですか？'))return;pushHistory();applyStoredPayload(manual);localStorage.removeItem(DRAFT_KEY);lastDraftSavedAt=0;dirty=false;selectedIds.clear();primaryId=null;renderPaperOptions();renderPalettes();renderTemplates();render();requestAnimationFrame(fitView);setStatus(`保存点へ復元 ${formatClock(Number(manual.savedAt)||0)}`)}

  document.querySelectorAll('[data-align]').forEach(b=>b.addEventListener('click',()=>align(b.dataset.align)));
  $('sampleBtn').onclick=loadTrialSample;$('resetBtn').onclick=resetLayout;$('restoreSavedBtn').onclick=restoreManualSave;$('backupBtn').onclick=exportJson;editSelectedBtn.onclick=()=>{if(selectedIds.size===1)openCardEditor([...selectedIds][0])};quickDuplicateBtn.onclick=duplicateSelected;quickDeleteBtn.onclick=deleteSelected;clearSelectionBtn.onclick=()=>{selectedIds.clear();primaryId=null;refreshSelection()};editForm.addEventListener('submit',e=>{if(e.submitter?.value==='cancel')return;commitCardEdit()});editDialog.addEventListener('close',()=>{editingCardId=null});
  $('undoBtn').onclick=undo;$('redoBtn').onclick=redo;$('duplicateBtn').onclick=duplicateSelected;$('deleteBtn').onclick=deleteSelected;hideBtn.onclick=hideSelected;lockBtn.onclick=toggleLock;snapBtn.onclick=()=>{snapEnabled=!snapEnabled;markDirty();render()};$('saveBtn').onclick=save;$('exportBtn').onclick=exportJson;$('importBtn').onclick=()=>importFile.click();importFile.onchange=()=>{const f=importFile.files?.[0];if(f)importJsonFile(f)};$('zoomInBtn').onclick=()=>setZoom(view.zoom*1.15);$('zoomOutBtn').onclick=()=>setZoom(view.zoom/1.15);$('fitBtn').onclick=fitView;paperSizeEl.onchange=()=>applyPaperSize(paperSizeEl.value);$('guideBtn').onclick=()=>{guideVisible=!guideVisible;markDirty();render()};extensionPackEl.onchange=()=>{activeExtension=extensionPackEl.value;renderPalettes();markDirty()};t